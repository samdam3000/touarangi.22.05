
services:
  - type: background
    name: touarangi-engine
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    autoDeploy: true
    schedule: "@every 30s"
